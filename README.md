# dlogs - docker logs with filtering

Utility that adds convenient filtering of docker logs.

## Installing

```bash
git clone https://github.com/tgaru/dlogs.git /tmp/dlogs && /bin/bash /tmp/dlogs/install.sh && rm -rf /tmp/dlogs
```

## Usage
```bash
dlogs
```